import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function PatentsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Patents</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Browse and search for patents.</p>
      </CardContent>
    </Card>
  );
}
