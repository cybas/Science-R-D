import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function MemoryPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Memory</CardTitle>
      </CardHeader>
      <CardContent>
        <p>View your saved notes, highlights, and findings.</p>
      </CardContent>
    </Card>
  );
}
